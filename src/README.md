# 源码目录规划

该目录目前不包含实现代码，仅用于固定后续模块边界。

- `app/`：应用装配、路由、依赖注入与全局生命周期。
- `features/`：项目、生成、帧工作区和导出等用户功能。
- `core/`：领域模型、业务规则、任务状态和用例契约。
- `infrastructure/`：AI 服务商、网络、存储和导出的具体实现。
- `shared/`：跨模块共享的 UI 基础、类型、错误和工具。

新增实现前，应先确认它属于稳定业务规则、用户功能还是外部系统实现，避免服务商专有逻辑进入 `core/` 或页面层。
