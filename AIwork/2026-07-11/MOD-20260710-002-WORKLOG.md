---
module_id: MOD-20260710-002
date: 2026-07-11
timezone: Asia/Hong_Kong
canonical_spec: ../2026-07-10/MOD-20260710-002-source-image-sequence-flow.md
status_at_start: Draft
status_at_end: Approved
commits:
  - 1f47b50
---

# MOD-20260710-002 工作记录

## 当日目标

- 补充图生序列帧的游戏工程化预设体系。
- 明确角色序列帧、场景序列帧及角色动作子类。
- 分离预设底词、用户描述和结构化动画参数的职责。

## 完成内容

- 新增角色序列帧和场景序列帧两种一级类型。
- 为角色类型增加待机、攻击、其他三个 MVP 动作预设。
- 定义公共工程底词、类型底词、动作底词、用户描述和负向约束的提示词合成顺序。
- 定义初始帧数、帧率、循环方式和对齐默认值。
- 明确帧率、帧数、循环方式、锚点和画布必须作为结构化字段保存。
- 要求生成任务保存源图、预设 ID/版本、编译提示词和最终有效参数快照。
- 更新 `GENERATION` 模块规划职责和主要失败模式。
- 用户明确批准 `MOD-20260710-002`，设计基线提交为 `1f47b50`。
- 同步正式 MVP、项目定义、路线图、系统架构、AI API 和模块索引。
- 将帧率、循环方式、画布和帧顺序明确为预览与导出的任务快照输入。

## 设计偏差

- 原 Draft 只把序列生成描述为“源图＋动作描述＋序列参数”。现扩展为“源图＋版本化预设＋用户描述＋结构化参数”。
- 帧率不作为单纯提示词要求，而是结构化播放和导出元数据；提示词只辅助表达时间连续性与工程化视觉约束。

## 测试与验证

- 已检查角色待机、攻击、其他和场景四类预设均具备默认参数与底词。
- 已检查本次扩展仍保持单一主文档，没有复制完整设计。
- 待用户批准后再同步长期产品、架构和 AI API 文档。

## 风险与阻塞

- 首个 AI 服务商尚未确定，实际支持的帧数和帧率可能需要映射。
- 角色背景模式尚未确定是否进入 MVP。
- 本模块仍处于 `Draft`，不得进入代码实现。

## 相关提交

- `1f47b50`：批准源图准备与序列帧工作流设计。

## 下一步

- 同步正式产品、MVP、系统架构、AI API 和模块索引文档。
- 保持产品模块为 `Planned`；技术栈与首个 AI 服务商确定后再进入代码实施。
